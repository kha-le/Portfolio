<%= render "layouts/errors", :object => @comment %>

<%= form_for [@skill, @comment] do |f| %>
  <%= f.label :author %>:
  <br>
  <%= f.text_field :author %>
  <br>
  <%= f.label :body %>:
  <br>
  <%= f.text_area :body, :cols => 40, :rows => 10 %>
  <hr>
  <%= f.submit %>
<% end %>
